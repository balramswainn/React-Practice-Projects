import React from 'react'

const UserContext = React.createContext()   //Creates a Context object using React.createContext().This allows you to share data globally across components without passing props manually at every level (avoids "prop drilling").

export default UserContext; //Exports the context so it can be used in other files.







